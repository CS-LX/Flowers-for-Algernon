# 项目开发规则

## 项目最高优先级定向

- **本项目是“六边形纪念碑谷”错视解谜游戏。** 产品方向是错视解谜，不是独立体素编辑器。三棱柱体素、Part、Object Tree 和局部体素编辑器都服务于关卡制作、白盒搭建和运行时 Debug。
- **产品方向不等于当前任务范围。** “玩法优先”只说明编辑器/工具不是最终产品，不能把用户点名的框架、基础设施或分步任务自动升级成必须立刻接一条可玩闭环。用户说“先做 X”时，交付 X 的边界，不为了演示去写 X 的应用。
- **当前轮次没有点名的功能，一律不算任务。** 用户说“clone / 基于它继续开发 / 看看进度”时，只允许同步仓库、阅读和汇报现状；不得自行挑选文档缺口、测试台词、fullscreen、过关闭环或其他未点名项开写。文档里的“当前缺口 / 今日目标 / 计划下一步”是菜单，不是开工令。任务边界含糊时必须先问用户要改哪一块，得到明确范围前禁止改代码。禁止用“交付可见进展”填空白。
- **向成熟的纪念碑谷式工作流看齐。** 关卡应围绕可走面导航节点、固定正交游戏相机、机关合法 Snap 状态、几何变化后的视觉连接评估和运行时可达路径组织；不得把普通体素编辑器或通用 NavMesh 的工作流当作默认方案。
- **不自己造轮子。** 优先复用已调研、已验证的成熟工作流和项目内已有基础设施；只有在没有可复用方案或为了明确的六边形三棱柱差异时，才新增专用实现。任何偏离成熟工作流的临时方案都必须明确标注为临时方案，不能升级为目标架构。
- **Path Graph 的目标架构是由关卡配置、机关 Snap 状态和固定游戏相机下的视觉连接评估共同生成当前有效连接。** 显式条件边可以作为固定连接、白盒临时兜底、设计者覆盖或 Debug 对照，但不能作为错视连接的长期主要配置方式。
- **寻路只消费当前有效 Path Graph。** 不得让 BFS、角色、Scene Node、Mesh 或物理碰撞反向定义关卡连接；逻辑 Graph、视觉连接评估和表现层必须保持职责分离。
- 开始涉及路径、机关、可达性或角色移动的开发前，必须先阅读 `docs/plans/hexagonal-monument-valley-technical-validation.md` 和 `docs/monument-valley-path-graph-research.md`，并以其中的目标架构为准。

## 当前轮次任务边界（硬闸）

写任何代码前，必须能用一句话回答：**用户这一轮点名要改什么？** 答不出，就停手提问。

允许写代码的条件，三条同时成立：

1. 用户这一轮明确说出了要改的对象、文件、功能或缺陷；
2. 准备改的文件/行为，能直接对应那句指令，而不是从文档、TODO、测试代码或“继续开发”里推出来的；
3. 没有把“让它更完整 / 接上闭环 / 补上缺口 / 先做正片”当作授权。

明确禁止：

- 把 `docs/plans/*`、`CLAUDE.md` 产品方向、技术验证计划、轻剧情计划里的未完成项，当成这一轮默认任务；
- 发现测试台词、fullscreen 降级、缺 HUD、缺导演登记后，未经用户点名就去“改成正式闭环”；
- clone / 同步 / 分析完成后，为了显得在继续开发而主动改玩法、剧情、UI 或编辑器；
- 用户只给仓库地址、token、截图或“基于它继续开发”时，自行决定下一刀砍哪里。

反例（2026-09-01）：用户要求 clone `Flowers-for-Algernon` 并基于它继续开发。正确动作是同步仓库后停下来问下一刀。错误动作是看见第一章还在播 `bannerTest` / `modalTest`，就去补 fullscreen 和开场/过关剧情。

## 开发警示

- **先定真相源，再写交互：** 先明确数据、状态和职责的唯一归属，UI、表现层和派生结果不得各自维护同一语义。
- **先做完整链路，再修局部症状：** 这条只用于排查已有功能的用户操作链路（点击、提交、反馈、下一帧）。它不是“任何新模块都必须先接到可玩演示”。引入库、总线、容器生命周期时，先按用户指定的边界落地；没有明确要求时，不要为了完整链路去写死默认应用。
- **先定任务边界，再写应用：** 用户要求实现系统/框架时，只交付该系统的 API 与生命周期。用现有对象接 Publisher/Subscriber、过关、点击演示，属于另一次明确任务。
- **不把临时方案升级为架构：** 未验证的猜测、兼容分支和便捷兜底必须标明边界；发现方向偏离时优先收敛模型并删除重复实现。
- **交付前区分“能运行”和“好使用”：** 除错误检查外，必须回归关键点击、命中、悬停、视觉反馈和状态同步，并如实报告尚未验证的部分。

## 预览与运行时验证

- 用户已确认预览正常时，以预览结果作为当前视觉正确性的基线；不得仅因其他验证环境失败就替换正常实现。
- 独立无头 Runtime 验证不是默认必需步骤，只有用户明确要求或任务确实需要时才使用。
- 预览、构建、LSP、独立 Runtime 属于不同层次的检查。发现环境差异时，先归因并记录差异，不得把工具环境错误扩展为项目代码错误。
- 一个 API 在某个 Runtime 中失败，只能证明该 API 在该环境不可用，不能据此推断其他 API 也失败。
- 任何会改变已正常视觉结果的替换，必须先保留原实现，并在修改后进行目标画面回归检查；出现纸片、圆柱、黑屏等回归时立即恢复基线。
- 验证工具报错时，先区分用户脚本错误、资源缺失、运行时能力差异和验证器自身问题，再决定是否修改代码。

## 渲染验证与 3D Overlay

- 不能根据单次黑屏、全黑截图或某个独立 Runtime 的 shader/resource 错误，直接判定 Viewport、RenderPath 或 Overlay 架构不可用。
- 验证多 Viewport 时，必须使用颜色明确不同的 MainScene/OverlayScene，并检查真实像素；默认 RenderPath 可能包含 `CLEAR_COLOR | CLEAR_DEPTH | CLEAR_STENCIL`，会把前一个 Viewport 清掉。
- 3D 置顶 Overlay 的正式验证链路是：Viewport 0 绘制 MainScene，Viewport 1 绘制 OverlayScene；Overlay RenderPath 的首个清屏命令只清 `CLEAR_DEPTH`，不能清 `CLEAR_COLOR`。
- 在确认该链路前，不得用 UI 屏幕空间线条替代真实 3D Overlay，也不得把 DebugRenderer、CustomGeometry、UI Overlay 多次试错后的现象泛化为引擎能力结论。
- 渲染结论必须区分：API 存在、脚本运行、Scene 创建、真实像素合成、用户预览；低层探针失败时，先检查探针是否真正执行了目标命令以及是否在截图帧前提前退出。

## 变更范围与回退安全

- 用户要求回退某个功能时，必须按功能边界回退，不得按提交时间粗暴 `reset --hard` 到更早提交；与目标功能无关的已验收功能必须保留。
- 推送过的提交不得通过强制重写远程历史来修复范围错误；应恢复正确基线后，用普通提交记录范围化修复。
- 回退前必须先检查提交差异，列出需要移除和必须保留的文件/hunk；回退后必须用 `git diff`、`git status` 和提交历史复核。
- 用户明确说“先别写代码”或要求先调研时，只允许读取文档、源码和运行证据；不得创建实验实现、临时 Overlay 或替换现有渲染链路。
- 渲染问题在证据闭合前不得连续切换 DebugRenderer、CustomGeometry、UI Overlay 等实现；每次方案替换必须先说明渲染层、深度策略、提交顺序和回退边界，并等待用户确认。
- 发现误回退后，首先恢复无关的已验收功能，再处理目标问题；不得把事故扩大为整体回退。

## 编辑器 Gizmo 规范

- 编辑器 Gizmo 需求必须先区分目标对象：Level Object Tree 的 Gizmo 操作 PartRoot；Part Voxel Editor 的 Gizmo 操作局部 Selection。后者必须通过 PendingTransform/History 更新 VoxelDocument，禁止只移动显示 Node。
- Part Editor 轴拖拽的第一版采用 Unity/Blender 的 XYZ 颜色约定：X 红、Y 绿、Z 蓝；使用轴约束拖拽平面和网格吸附，不得把一次性按钮移动误当成完整 Gizmo 交互。

## Git 提交与推送

- 远程仓库使用不含凭据的标准 HTTPS URL，禁止把 token 写入 remote URL、Git 配置、提交内容或日志。
- GitHub token 保存在项目本地 `secret/github_token`，`secret/` 必须持续受 `.gitignore` 排除。
- 用户说“提交并且推送”时，从 `secret/github_token` 临时读取凭据完成认证；认证数据只用于该次命令，不持久化到 Git 配置。
- **用户说“提交并且推送”时直接提交并推送。** 不要先做长时间的范围核对、diff 通读、secret 复查、远程 URL 复查或其它无意义检查；已暂存的改动直接 commit，未暂存的相关改动直接 add 后 commit，然后立刻 push。禁止为“再确认一遍”卡住数分钟。
